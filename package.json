{
  "private": true,
  "name": "mobouzer-monorepo",
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "app": "yarn workspace @mobouzer/app"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
